# 文件上传安全优化建议

## 文档状态

- 最近更新：2026-07-15
- 文件上传安全 V1 已完成代码实现与 Go 自动化测试。
- 当前 OpenSpec change：`harden-file-upload-security`
- 当前阶段：等待文档验收、主 specs 同步和 change 归档。

本文只记录改造背景、实施概要、版本边界和后续优化方向，不重复复制 OpenSpec 中的完整接口行为、状态矩阵和验收场景。

## 改造背景

改造前，管理员普通文件上传和用户头像上传主要依赖文件大小、客户端文件名、扩展名及声明 MIME，无法可靠阻止伪装文件、损坏图片、危险 Office 容器、超大 multipart 请求和外部头像 URL。

V1 的目标是在不引入外部恶意文件扫描服务的前提下，为两类上传建立一致、可审计、可迁移且可自动化测试的格式策略安全边界。

## V1 实施概要

V1 已同时加固以下入口：

- 管理员普通文件上传：`POST /api/admin/files`
- 用户头像上传：`POST /api/user/avatar`

主要成果：

- 在 multipart 解析前限制请求体，并严格限制为单个名为 `file` 的文件 part。
- 使用服务端生成的 object key，清洗展示文件名，不信任客户端路径、扩展名或声明 MIME。
- 普通文件采用严格类型白名单和内容验证，Office 文件增加受资源上限保护的 Office Open XML 校验。
- 头像完整解码后移除元数据、限制尺寸并重新编码为标准 JPEG 或 PNG。
- 文件记录增加验证状态、检测 MIME、策略版本、验证时间和稳定失败原因。
- 历史文件和头像只回填验证状态；迁移期间不读取、解码、删除或重写历史 MinIO 对象。
- 文件下载、图片预览和头像读取改为应用受控接口，不再向客户端暴露原始 MinIO 访问地址。
- 上传成功、策略拒绝和可安全分类的失败会记录结构化审计元数据，但不记录文件内容、object key、签名 URL、存储凭据或底层解析错误。
- 配置项写入 `config.yaml`，非法值会导致服务启动失败。

V1 普通文件允许 JPEG、PNG、WebP、PDF、DOCX、XLSX、PPTX、UTF-8 TXT 和 UTF-8 CSV。该结论表示文件通过 `file-upload-v1` 格式策略验证，不表示已经完成病毒或恶意内容扫描。

## 权威规格与实现依据

V1 的完整可观察行为、错误分类、状态转换和验收场景以当前 OpenSpec change 为准：

- `openspec/changes/harden-file-upload-security/proposal.md`
- `openspec/changes/harden-file-upload-security/design.md`
- `openspec/changes/harden-file-upload-security/specs/file-management/spec.md`
- `openspec/changes/harden-file-upload-security/specs/user-management/spec.md`
- `openspec/changes/harden-file-upload-security/specs/logging/spec.md`
- `openspec/changes/harden-file-upload-security/tasks.md`

change 同步后，长期权威规格切换为：

- `openspec/specs/file-management/spec.md`
- `openspec/specs/user-management/spec.md`
- `openspec/specs/logging/spec.md`

## V1 边界

V1 不包含：

- 外部恶意文件扫描或病毒查杀。
- 通用压缩包、旧版 Office、宏文档、脚本和可执行文件上传。
- CSV/XLSX 业务数据导入。
- 多文件或批量上传。
- 上传限流和用户存储配额。
- 启动时批量扫描历史 MinIO 对象。

新增文件类型不能只通过修改配置开放，必须经过需求、风险、OpenSpec 和自动化测试评审。

## V2 及后续优化

以下能力保留为 V2 或更后续版本的独立 change，不在 V1 中隐式开启：

1. **ClamAV 或外部恶意文件扫描**
   - 评估 ClamAV、ICAP 或云扫描服务。
   - 明确扫描超时、不可用、误报、重试和 fail-open/fail-closed 策略。
   - 增加扫描状态、签名库版本、监控和告警，但不记录文件内容。
2. **隔离区**
   - 未扫描、扫描中、可疑和确认恶意文件进入不可下载、不可预览的隔离存储。
   - 设计管理员复核、释放、删除和审计流程。
3. **批量重新验证**
   - 为历史文件提供可续跑、可限速、可重试、可观测的后台任务。
   - 单个对象失败不得阻断整个批次，且不得在迁移或服务启动期间同步扫描。
4. **上传限流**
   - 按用户、IP、接口或租户限制请求频率和并发数。
   - 对超限结果使用稳定错误码并纳入监控。
5. **存储配额**
   - 按用户、组织或租户限制文件数量和累计容量。
   - 明确上传、删除、轮转、补偿失败和孤立对象对配额的影响。
6. **批量上传**
   - 在单文件 V1 稳定后再设计批量请求、部分失败、幂等和逐文件审计语义。
   - 每个文件仍必须独立通过完整安全策略，不能以批次结果绕过验证。
7. **新增文件类型评审**
   - 每种新增类型先建立 GitHub Issue 和独立 OpenSpec change。
   - 明确业务用途、扩展名/MIME/内容验证、下载与预览策略、危险样本和回归测试。
   - 压缩包、旧版 Office、宏文档、脚本和可执行文件默认继续拒绝；只有具备隔离、扫描和审计方案后才重新评估。
