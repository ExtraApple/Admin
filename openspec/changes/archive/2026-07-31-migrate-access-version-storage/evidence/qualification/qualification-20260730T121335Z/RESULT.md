# 三小时授权版本资格验收结果

状态：通过

核验时间：2026-07-30 23:19:46 +08:00

## 运行身份与环境

- Run ID：`qualification-20260730T121335Z`
- 开始时间：2026-07-30 20:13:37 +08:00
- 完成时间：2026-07-30 23:13:39 +08:00
- 连续运行时间：3 小时 2.4109524 秒
- 数据库：Windows 本地隔离真实 MySQL，
  `127.0.0.1:23306/admin_rehearsal`
- Redis：Docker Redis 容器 `redis`，测试连接 `127.0.0.1:6379`
- 包装进程退出码：0
- 最终判定：`qualified=true`
- 运行制品 SHA-256：
  `0AD925B7C897DDD67E6E3913653E561B1A987AF8844EA7055451C66C878776E1`

运行期间没有连接、停止、重启或修改
`127.0.0.1:3306` 上的正式 MySQL。MySQL、Redis、工作用户和 JWT 凭据只通过
进程环境注入，没有写入本证据目录。

## 固定门槛结果

| 门槛 | 结果 |
| --- | --- |
| 连续运行至少 3 小时 | 通过：3 小时 2.4109524 秒 |
| 至少 100,000 次混合操作 | 通过：125,000 次 |
| 至少 8 个并发工作单元 | 通过：8 |
| 至少 3 次应用子进程重启 | 通过：3 次 |
| 开始、最长每 15 分钟、重启后及结束扫描 | 通过：17 次 |
| 所有扫描一致性差异为 0 | 通过：一致性差异：0 |
| 非预期操作失败为 0 | 通过：0 |
| 非预期镜像写失败为 0 | 通过：非预期镜像写失败：0 |
| 成功签发 Token 后缺行为 0 | 通过：Token 签发后缺行：0 |
| 版本提升后缺行为 0 | 通过：版本提升后缺行：0 |
| 每阶段真实原子回滚故障注入 | 通过：4 次 |
| Windows 防休眠取得并释放 | 通过 |

17 次扫描包括：

- 初始扫描 1 次；
- 最长每 15 分钟的周期扫描 12 次；
- 受控重启后扫描 3 次；
- 最终扫描 1 次。

每次扫描均得到：

```text
differences=0
legal_post_cutoff_missing=0
```

## 混合认证授权工作量

| 操作 | 次数 |
| --- | ---: |
| Access Token 校验 | 25,001 |
| Refresh Token | 25,002 |
| 黑名单组合验证 | 25,001 |
| 会话失效与旧 Token 拒绝 | 24,999 |
| 登录 | 24,997 |
| 合计 | 125,000 |

四个真实应用子进程阶段分别完成 31,250 次操作。每个阶段都执行一次真实 MySQL
CHECK 约束故障注入，验证新表版本提升和旧字段镜像任一步失败时事务不会部分提交。
四个阶段的非预期失败和缺行指标均为 0。

## 运行后事件触发门禁

用户在正式运行结束后要求继续本 Change，因此按任务 8.7 于
2026-07-30 再次执行：

- `go test ./service ./router -count=1`：通过；
- 真实 MySQL 并发、行锁、死锁重试、镜像回滚和软删除事务 8 个场景：通过；
- 真实 Docker Redis Access Token、Refresh Token 和黑名单组合门禁：通过；
- 真实 MySQL 只读一致性扫描：通过，
  `cutoff_user_id=1 consistency_differences=0 legal_post_cutoff_missing=0`。

原始日志位于：

```text
evidence/observation/2026-07-30-exit-preflight/
```

## 证据完整性

- `policy.json`：正式三小时、100,000 操作、8 并发、3 重启和 15 分钟扫描策略；
- `events.jsonl`：启动、17 次扫描、四个阶段、三次重启和完成事件；
- `phase-01.json` 至 `phase-04.json`：各阶段累计结果；
- `phase-01.log` 至 `phase-04.log`：真实 MySQL 故障注入日志；
- `launcher-runtime.json`：Windows 防休眠取得及释放证据；
- `launcher-completion.json`、`exit-code.txt`：独立包装进程完成且退出码为 0；
- `artifact-sha256.txt`：正式运行制品摘要；
- `summary.json`：最终资格汇总。

正式运行制品在核验时重新计算的 SHA-256 与
`artifact-sha256.txt` 完全一致。

## 结论

本次运行满足任务 8.6 和 9.1 的全部固定资格门槛。它是固定工作量、并发、重启、
扫描和故障注入条件下的可重复发布资格证据，不声明与真实生产连续 7 日运行具有
统计等价性。
