# 2026-07-30 授权版本旧字段退出资格确认

状态：任务 9.1 通过，任务 9.2 待发布负责人确认

核验时间：2026-07-30 23:20 +08:00

## 前置里程碑

- 切换里程碑：通过；
- 三小时资格验收：通过；
- 本次事件触发退出前置门禁：通过。

## 三小时资格门槛

- 连续运行：3 小时 2.4109524 秒；
- 混合认证授权操作：125,000 次；
- 并发：8 个并发工作单元；
- 重启：3 次受控应用子进程重启；
- 扫描：17 次一致性扫描，全部差异为 0；
- 非预期操作失败：0；
- 非预期镜像写失败：0；
- Token 签发后缺行：0；
- `EnsureAndIncrement` 后缺行：0；
- 原子回滚故障注入：4 次通过；
- 登录、Access Token、Refresh Token、黑名单、会话失效和旧 Token 拒绝均有
  实际混合流量覆盖。

正式运行汇总：

```text
evidence/qualification/qualification-20260730T121335Z/summary.json
```

正式核验报告：

```text
evidence/qualification/qualification-20260730T121335Z/RESULT.md
```

## 运行后关键回归

用户要求继续本 Change 后，已再次执行并归档：

- 关键认证授权回归：通过；
- 真实 MySQL 并发与事务门禁：通过；
- 真实 Redis 组合门禁：通过；
- 只读一致性扫描：差异 0。

证据：

```text
evidence/observation/2026-07-30-exit-preflight/
```

## 任务边界

任务 9.1 的固定资格门槛已经全部满足。

不再回滚到读取旧字段的确认：尚未取得。

因此：

- 任务 9.2 保持未完成；
- 在发布负责人明确确认前，不得开始任务 9.3；
- 当前仍保留 `users.token_version`、观察期镜像写和旧代码版本回滚能力。
