# 2026-07-30 授权版本旧代码回滚弃用审批

状态：通过

确认时间：2026-07-30 23:34 +08:00

确认人：项目发布负责人（用户）

## 确认内容

发布负责人确认不再需要回滚到读取 `users.token_version` 的旧代码版本。

确认来源：发布负责人在当前实施会话中明确同意。

## 已知影响

- 允许继续任务 9.3，执行退出前最后一次新旧版本一致性扫描；
- 退出版本发布后，只允许回滚到仍读取 `user_access_versions` 的代码版本；
- 停止镜像写并移除旧字段运行时依赖后，不再恢复旧代码版本的流量；
- 物理删列后恢复旧字段需要独立数据库变更，不属于常规应用回滚。

## 数据库操作边界

本地实施中的独立删列 DDL 仅允许作用于 Windows 本地隔离 MySQL：

```text
127.0.0.1:23306/admin_rehearsal
```

不得连接、停止、重启或修改 `127.0.0.1:3306` 的既有 `MySQL80` 实例。
