# 2026-07-30 授权版本观察期第 3 次任务触发记录

状态：当次前置门禁通过，观察期未完成

执行时间：2026-07-30 17:14:43 +08:00

触发策略：事件触发，不设置固定执行时间

触发原因：用户要求继续执行本 Change

观察回归日：3

连续有效指标观察日：0/7

仓库已有 2026-07-28、2026-07-29 和 2026-07-30 三次按日期归档的观察回归
证据，但当前应用未持续运行，授权版本指标是应用进程内计数器，进程内指标没有跨进程历史持久化。
因此，这三次回归不能证明任意一个自然日内的持续运行镜像写失败数为 0，也不能
组成连续 7 个自然日的有效指标观察。

## 退出门槛状态

- 完整发布周期：未完成
- 镜像写失败连续 7 日：未满足
- 一致性扫描差异数：0
- 关键认证授权回归：通过
- 真实 MySQL 并发与事务门禁：通过
- 成功签发 Token 后缺行回归：通过
- `EnsureAndIncrement` 后缺行回归：通过

本记录只证明 2026-07-30 本次任务触发前最终执行的回归和一致性扫描结果。
本次任务触发门禁不能替代 8.6 的完整发布周期和连续至少 7 个自然日指标观察。

## 验证环境

- 数据库：Windows 本地隔离真实 MySQL，
  `127.0.0.1:23306/admin_rehearsal`。
- 正式 Windows 服务 `MySQL80` 在整个过程中保持 Running，端口 3306 保持监听；
  未连接、停止或重启 `127.0.0.1:3306/MySQL80`。
- 隔离实例只使用
  `%TEMP%\admin-access-version-rehearsal-20260728\mysql-data`，并显式使用
  `--no-defaults`、端口 23306 和 `--mysqlx=0`。
- MySQL 不在 Docker 中。Redis 在 Docker 中；本次 MySQL 观察门禁没有启动、
  停止或修改 Redis。
- 数据库密码只从临时 `.env.shared` 注入当前 PowerShell 测试进程，未输出或
  写入仓库。

## 前置门禁执行结果

### 关键认证授权回归

```powershell
go test ./service ./router -count=1
```

最终结果：

```text
ok  	admin/service
ok  	admin/router
```

该套件覆盖登录、Refresh Token、JWT、账号禁用、用户软删除与恢复、Kick、
授权变化和授权版本指标回归。成功签发 Token 后缺行与
`EnsureAndIncrement` 后缺行的指标测试均通过。

### 隔离凭据恢复记录

第一次真实 MySQL 门禁在打开数据库时返回 1045，8 个业务场景均未开始执行。
使用 MySQL 官方客户端对同一 `127.0.0.1:23306` 验证后，确认隔离实例和原凭据
可用，问题限于 Go DSN 注入兼容性。

随后只在 23306 隔离实例中轮换 `admin_rehearsal` 的随机 DSN 安全密码，并同步
更新临时 `.env.shared`；没有修改正式 3306 实例。MySQL 官方客户端重新认证
成功后，完整重跑真实 MySQL 门禁并通过。未把第一次认证失败记录为业务测试失败，
也未把修复前结果计入最终门禁。

### 真实 MySQL 并发与事务门禁

```powershell
$env:ADMIN_TEST_MYSQL_DSN = "<从临时隔离凭据注入>"
go test -tags=mysql_integration ./service `
  -run '^TestMySQL(AccessVersionRepository|DeletingUser)' `
  -count=1 -v
```

最终结果：8 个场景全部通过，包括并发首次初始化、并发提升、行锁等待、初始化与
提升竞态、真实死锁重试、镜像写失败回滚、软删除原子提交和镜像失败业务回滚。

### 只读一致性扫描

```powershell
$env:ADMIN_TEST_MYSQL_DSN = "<从临时隔离凭据注入>"
go test -tags='mysql_integration,rollback_preflight' ./service `
  -run '^TestMySQLAccessVersionRollbackPreflightConsistencyScan$' `
  -count=1 -v
```

最终结果：

```text
rollback preflight passed: cutoff_user_id=1 consistency_differences=0 legal_post_cutoff_missing=0
```

扫描为只读，未修改演练数据库。

## OpenSpec 任务状态

- 未勾选 8.6：尚无一个完整发布周期，也没有连续 7 个自然日可验证的持久化
  镜像写失败指标。
- 未勾选 8.7：事件触发门禁策略已经固化并在本次执行，但整个观察期尚未完成。
- 9.1–9.10 仍受 8.6、8.7 和发布负责人确认约束，不得提前执行旧字段退出。
