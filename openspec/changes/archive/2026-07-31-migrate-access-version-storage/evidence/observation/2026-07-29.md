# 2026-07-29 授权版本观察期第 2 日记录

状态：当日回归通过，观察期未完成

执行时间：2026-07-29 22:59:20 +08:00

观察日：2/7

## 退出门槛状态

- 完整发布周期：未完成
- 镜像写失败连续 7 日：未满足
- 一致性扫描差异数：0
- 关键认证授权回归：通过
- 真实 MySQL 并发与事务门禁：通过
- 成功签发 Token 后缺行回归：通过
- `EnsureAndIncrement` 后缺行回归：通过

本记录只证明 2026-07-29 当日执行的回归和一致性扫描结果。授权版本指标是
应用进程内计数器，演练应用已经停止，仓库当前没有跨进程持久化指标导出，因此
不能从本次测试恢复或推断此前运行时段的镜像写失败数。本记录不把测试通过解释为
连续 7 天镜像写失败数为 0，也不声称已经完成一个真实发布周期。

## 验证环境

- 数据库：Windows 本地隔离真实 MySQL，
  `127.0.0.1:23306/admin_rehearsal`。
- 未连接、停止或重启 `127.0.0.1:3306/MySQL80`。
- MySQL 不在 Docker 中；数据库密码只从
  `%TEMP%\admin-access-version-rehearsal-20260728\.env.shared`
  注入当前 PowerShell 测试进程，未输出或写入仓库。
- Redis 在 Docker 中；真实 Redis 组合门禁已经作为任务 10.6 独立完成。
  本次观察日门禁不重复启动 Redis，也不把 Redis 环境与 MySQL 环境混用。

## 当日执行结果

### 关键认证授权回归

```powershell
go test ./service ./router -count=1
```

结果：

```text
ok  	admin/service
ok  	admin/router
```

该套件覆盖登录、Refresh Token、JWT、用户状态、软删除、Kick、授权变化和
授权版本指标回归。成功签发 Token 后缺行与 `EnsureAndIncrement` 后缺行的
指标测试均通过。

### 真实 MySQL 并发与事务门禁

```powershell
$env:ADMIN_TEST_MYSQL_DSN = "<从临时隔离凭据注入>"
go test -tags=mysql_integration ./service `
  -run '^TestMySQL(AccessVersionRepository|DeletingUser)' `
  -count=1 -v
```

结果：8 个场景全部通过，包括并发首次初始化、并发提升、行锁等待、初始化与
提升竞态、真实死锁重试、镜像写失败回滚、软删除原子提交和镜像失败业务回滚。

### 只读一致性扫描

```powershell
$env:ADMIN_TEST_MYSQL_DSN = "<从临时隔离凭据注入>"
go test -tags='mysql_integration,rollback_preflight' ./service `
  -run '^TestMySQLAccessVersionRollbackPreflightConsistencyScan$' `
  -count=1 -v
```

结果：

```text
rollback preflight passed: cutoff_user_id=1 consistency_differences=0 legal_post_cutoff_missing=0
```

扫描为只读；未修改演练数据库。

## OpenSpec 任务状态

- 未勾选 8.6：尚未连续观察至少一个完整发布周期和 7 天镜像写失败数。
- 未勾选 8.7：当前只有观察期第 1 日和第 2 日证据，尚未完成整个观察期的持续回归。
- 9.1–9.10 仍受 8.6、8.7 和发布负责人确认约束，不得提前执行旧字段退出。
