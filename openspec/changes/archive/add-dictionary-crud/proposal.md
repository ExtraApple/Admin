# add-dictionary-crud

## Why

后台系统需要统一管理可配置枚举，避免把用户状态、文件类型、消息类型等选项写死在代码里。

## What Changes

- 新增字典类型 CRUD。
- 新增字典条目 CRUD。
- 新增按字典类型编码获取可用条目接口，供前端下拉框使用。
- 字典类型删除时同步删除该类型下的条目。

## Out of Scope

- 不做多语言字典。
- 不做字典缓存。
- 不做前端页面。
